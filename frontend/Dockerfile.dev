FROM node:20-alpine

WORKDIR /app

# Install deps into the image layer (first run will still do npm install into the mounted volume)
COPY package.json package-lock.json* ./
RUN npm ci || npm install

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
