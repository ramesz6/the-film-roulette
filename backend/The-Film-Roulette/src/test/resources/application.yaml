spring:
  application:
    name: The-Film-Roulette

  datasource:
    url: jdbc:h2:mem:mydb
    username: sa
    password: password
    driver-class-name: org.h2.Driver

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

jwt:
  secret: somestupidvaluenotrealyoloagainsomestupidvaluenotrealyolo

tmdb:
  api:
    key: NOT_REALLY_A_TMDB_API_KEY

cors:
  urls: ${CORS_URLS:http://localhost:5173}